<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    android:background="@android:color/white">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Avatar -->
        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/iv_reviewer_avatar"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/perfil"
            app:civ_border_width="2dp"
            app:civ_border_color="#E0E0E0"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Verified Badge -->
        <ImageView
            android:id="@+id/iv_verified_badge"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_check_circle"
            app:tint="#4CAF50"
            android:layout_marginStart="-8dp"
            android:layout_marginTop="-8dp"
            android:background="@drawable/circle_background_white"
            app:layout_constraintTop_toTopOf="@id/iv_reviewer_avatar"
            app:layout_constraintEnd_toEndOf="@id/iv_reviewer_avatar" />

        <!-- Reviewer Name -->
        <TextView
            android:id="@+id/tv_reviewer_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Adrián López"
            android:textSize="16sp"
            android:textColor="@android:color/black"
            android:textStyle="bold"
            android:fontFamily="@font/poppins_semibold"
            android:layout_marginStart="12dp"
            app:layout_constraintTop_toTopOf="@id/iv_reviewer_avatar"
            app:layout_constraintStart_toEndOf="@id/iv_reviewer_avatar"
            app:layout_constraintEnd_toStartOf="@id/tv_rating" />

        <!-- Location -->
        <TextView
            android:id="@+id/tv_reviewer_location"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Lima, Perú"
            android:textSize="13sp"
            android:textColor="@android:color/darker_gray"
            android:layout_marginStart="12dp"
            app:layout_constraintTop_toBottomOf="@id/tv_reviewer_name"
            app:layout_constraintStart_toEndOf="@id/iv_reviewer_avatar"
            app:layout_constraintEnd_toStartOf="@id/rating_stars_container" />

        <!-- Rating Number -->
        <TextView
            android:id="@+id/tv_rating"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="4.9"
            android:textSize="16sp"
            android:textColor="#FF5722"
            android:textStyle="bold"
            android:fontFamily="@font/poppins_bold"
            app:layout_constraintTop_toTopOf="@id/tv_reviewer_name"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Rating Stars -->
        <LinearLayout
            android:id="@+id/rating_stars_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            app:layout_constraintTop_toBottomOf="@id/tv_rating"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Review Text -->
        <TextView
            android:id="@+id/tv_review_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="El servicio es excelente y me encantaron las instalaciones. ¡Buen trabajo!"
            android:textSize="14sp"
            android:textColor="@android:color/black"
            android:lineSpacingExtra="4dp"
            android:layout_marginTop="12dp"
            app:layout_constraintTop_toBottomOf="@id/iv_reviewer_avatar"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Date -->
        <TextView
            android:id="@+id/tv_review_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="2 días atrás"
            android:textSize="12sp"
            android:textColor="@android:color/darker_gray"
            android:layout_marginTop="8dp"
            app:layout_constraintTop_toBottomOf="@id/tv_review_text"
            app:layout_constraintStart_toStartOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>
<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="4dp">

    <!-- Contenedor de borde (opcional, se puede mostrar/ocultar programáticamente) -->
    <View
        android:id="@+id/border_indicator"
        android:layout_width="68dp"
        android:layout_height="48dp"
        android:background="@drawable/thumbnail_border_selected"
        android:visibility="gone" />

    <!-- CardView principal -->
    <androidx.cardview.widget.CardView
        android:id="@+id/card_thumbnail_container"
        android:layout_width="60dp"
        android:layout_height="40dp"
        android:layout_gravity="center"
        app:cardCornerRadius="8dp"
        app:cardElevation="2dp"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?android:attr/selectableItemBackground">

        <ImageView
            android:id="@+id/iv_thumbnail"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="Miniatura de imagen del hotel" />

    </androidx.cardview.widget.CardView>

</FrameLayout>
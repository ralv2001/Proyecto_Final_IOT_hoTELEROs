<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="4dp"
    android:paddingBottom="4dp">

    <!-- Date header -->
    <TextView
        android:id="@+id/tvDateHeader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="12 Abril"
        android:textSize="12sp"
        android:textColor="#9E9E9E"
        android:padding="8dp"
        android:background="@drawable/bg_date_header"
        android:layout_marginBottom="8dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/systemMessageContainer"
        app:layout_constraintVertical_chainStyle="packed"
        android:visibility="visible"/>

    <!-- System message container -->
    <LinearLayout
        android:id="@+id/systemMessageContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        app:layout_constraintTop_toBottomOf="@id/tvDateHeader"
        app:layout_constraintBottom_toBottomOf="parent">

        <TextView
            android:id="@+id/tvSystemMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Este chat ha sido cerrado"
            android:textSize="13sp"
            android:textColor="#757575"
            android:background="@drawable/bg_message_system"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingTop="8dp"
            android:paddingBottom="8dp"
            android:fontFamily="@font/inter_regular"
            tools:text="Este chat ha sido cerrado"/>

        <TextView
            android:id="@+id/tvTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="12:30"
            android:textSize="10sp"
            android:textColor="#9E9E9E"
            android:layout_marginTop="4dp"
            tools:text="12:30"/>
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
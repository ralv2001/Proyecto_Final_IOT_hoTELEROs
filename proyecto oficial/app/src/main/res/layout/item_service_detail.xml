<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_service"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:strokeColor="@color/light_gray"
    app:strokeWidth="1dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Contenedor del icono con fondo circular -->
        <androidx.cardview.widget.CardView
            android:id="@+id/fl_service_detail_container"
            android:layout_width="64dp"
            android:layout_height="64dp"
            app:cardCornerRadius="32dp"
            app:cardBackgroundColor="@color/light_orange"
            app:cardElevation="0dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent">

            <ImageView
                android:id="@+id/iv_service_detail_icon"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="16dp"
                android:src="@drawable/ic_wifi"
                android:contentDescription="Icono de servicio"
                app:tint="@color/orange_dark"/>
        </androidx.cardview.widget.CardView>

        <!-- Información del servicio -->
        <TextView
            android:id="@+id/tv_service_detail_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="WiFi de alta velocidad"
            android:textSize="18sp"
            android:textColor="@android:color/black"
            android:fontFamily="@font/poppins_medium"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="8dp"
            app:layout_constraintTop_toTopOf="@id/fl_service_detail_container"
            app:layout_constraintStart_toEndOf="@id/fl_service_detail_container"
            app:layout_constraintEnd_toStartOf="@id/tv_service_detail_price" />

        <!-- Etiqueta de precio o "Incluido" -->
        <TextView
            android:id="@+id/tv_service_detail_price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Incluido"
            android:textSize="14sp"
            android:textColor="@color/orange_dark"
            android:background="@drawable/bg_price_free"
            android:paddingHorizontal="12dp"
            android:paddingVertical="4dp"
            app:layout_constraintTop_toTopOf="@id/fl_service_detail_container"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Botón para expandir/contraer descripción -->
        <ImageView
            android:id="@+id/iv_expand_service"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:padding="8dp"
            android:src="@android:drawable/arrow_down_float"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Expandir descripción"
            app:layout_constraintTop_toBottomOf="@id/tv_service_detail_name"
            app:layout_constraintStart_toEndOf="@id/fl_service_detail_container"
            android:layout_marginStart="8dp"
            android:layout_marginTop="4dp"
            app:tint="@color/orange_primary" />

        <!-- Contenedor para la descripción expandible -->
        <androidx.cardview.widget.CardView
            android:id="@+id/description_container"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:visibility="gone"
            app:cardBackgroundColor="@color/light_orange_bg"
            app:cardCornerRadius="8dp"
            app:cardElevation="0dp"
            app:layout_constraintTop_toBottomOf="@id/iv_expand_service"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <!-- Descripción del servicio -->
            <TextView
                android:id="@+id/tv_service_detail_description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Conectarse a nuestra red inalámbrica en todas las áreas del establecimiento con velocidad premium."
                android:textSize="14sp"
                android:textColor="#555555"
                android:padding="12dp"/>
        </androidx.cardview.widget.CardView>

        <!-- Indicador de ahorro para servicios condicionales -->
        <TextView
            android:id="@+id/tv_service_conditional_hint"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="*Disponible gratis con una reserva mínima de S/. 350.00"
            android:textSize="12sp"
            android:textColor="@color/orange_accent"
            android:drawableStart="@drawable/ic_info_outline"
            android:drawablePadding="4dp"
            android:gravity="center_vertical"
            android:layout_marginTop="12dp"
            android:visibility="gone"
            app:layout_constraintTop_toBottomOf="@id/description_container"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Separador horizontal -->
        <View
            android:id="@+id/divider"
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:background="@color/light_gray"
            android:layout_marginTop="12dp"
            app:layout_constraintTop_toBottomOf="@id/tv_service_conditional_hint"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:visibility="gone"/>

        <!-- Contenedor de acciones (botones/chips) -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/actions_container"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            app:layout_constraintTop_toBottomOf="@id/divider"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <!-- Chip incluido en reserva (para servicios gratuitos) -->
            <com.google.android.material.chip.Chip
                android:id="@+id/chip_included_service"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Incluido en la reserva"
                android:textSize="12sp"
                app:chipBackgroundColor="@color/light_orange"
                app:chipStrokeColor="@color/orange_dark"
                app:chipStrokeWidth="1dp"
                app:chipIcon="@drawable/ic_check_circle"
                app:chipIconEnabled="true"
                app:chipIconTint="@color/orange_dark"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                android:visibility="visible"/>

            <!-- Botón para añadir servicios pagados -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_add_to_cart"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Añadir al carrito"
                android:textAllCaps="false"
                android:textSize="14sp"
                android:fontFamily="@font/poppins_medium"
                app:icon="@drawable/ic_add_circle"
                app:iconGravity="textStart"
                app:cornerRadius="18dp"
                app:backgroundTint="@color/orange_primary"
                android:visibility="gone"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />
        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>